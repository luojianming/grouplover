<div class="nav-menu">
  <%= form_tag invitations_path, :method => "get" do %>
  <ul class="container">


    <li class="dropdown">
    <%= text_field_tag :sex, params[:sex], :id => "sex_value", :type => "hidden"%>
      <a href="#"  class="dropdown-toggle" data-toggle="dropdown">
        <% if params[:sex]==nil || params[:sex].size==0%>
          <% if current_user != nil %>
            <%= current_user.profile.sex == "男" ? "女" : "男" %>
          <% else %>
            性别不限
          <% end %>
        <% else %>
          <%= params[:sex] %>
        <% end %>
        <b class="caret"></b>
      </a>
      <ul class="dropdown-menu" id="swatch-menu">
        <%= render 'layouts/sex' %>
        <li class="divider"></li>
      </ul>
    </li>

    <li class="dropdown">
    <%= text_field_tag :city, params[:city], :id => "city_value", :type => "hidden"%>
      <a href="#"  class="dropdown-toggle" data-toggle="dropdown">
        <% if params[:city]==nil || params[:city].size==0%>
          全国
        <% else %>
          <%= params[:city] %>
        <% end %>
        <b class="caret"></b>
      </a>
      <ul class="dropdown-menu" id="swatch-menu">
        <%= render 'layouts/hot_cities' %>
        <li class="divider"></li>
      </ul>
    </li>


    <li class="dropdown">
      <%= text_field_tag :time, params[:time], :id => "time_value", :type => "hidden"%>
      <a href="#"  class="dropdown-toggle" data-toggle="dropdown">
        <% if params[:time]==nil || params[:time].size==0%>
          时间不限
        <% else %>
          <%= params[:time] %>
        <% end %>
        <b class="caret"></b>
      </a>
      <ul class="dropdown-menu" id="swatch-menu">
        <%= render 'layouts/active_time' %>
        <li class="divider"></li>
      </ul>
    </li>


    <li class="dropdown">
      <%= text_field_tag :member_counts, params[:member_counts], :id => "member_counts_value", :type => "hidden"%>
      <a href="#"  class="dropdown-toggle" data-toggle="dropdown">
        <% if params[:member_counts]==nil || params[:member_counts].size==0%>
          人数不限
        <% else %>
          <%= params[:member_counts] %>
        <% end %>
        <b class="caret"></b>
      </a>
      <ul class="dropdown-menu" id="swatch-menu">
        <%= render 'layouts/member_counts' %>
        <li class="divider"></li>
      </ul>
    </li>
    <li><%= link_to '全部', root_path %></li>
  </ul>
  <input type="submit" value="搜索" name="commit" id="invitations_search_submit" style="display: none">
<% end %>
</div><!--navbar navbar-fixed-top-->
