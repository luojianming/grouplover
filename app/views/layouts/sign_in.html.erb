<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "爱组局" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "爱组局" %>">
    <%= stylesheet_link_tag "log","bootstrap_and_overrides" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
